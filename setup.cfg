[tool:pytest]
testpaths = src/
addopts = --cov=src --cov-report=term --cov-report=lcov:.coverage-dir/lcov.info --cov-report=html:.coverage-dir/html
required_plugins = pytest-cov

[coverage:run]
omit = **/*/tests/*
branch = true
data_file = .coverage

[coverage:report]
fail_under = 100
